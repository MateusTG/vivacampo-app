# =============================================================================
# VivaCampo - Configuração de Produção AWS
# =============================================================================
# Copie este arquivo para .env.prod e preencha os valores
# NUNCA commite o arquivo .env.prod no Git!
# =============================================================================

# -----------------------------------------------------------------------------
# TAG DA IMAGEM DOCKER
# -----------------------------------------------------------------------------
TAG=latest

# -----------------------------------------------------------------------------
# BANCO DE DADOS (Neon PostgreSQL)
# -----------------------------------------------------------------------------
# Conexão com banco Neon (serverless PostgreSQL)
# Formato: postgresql+psycopg://user:password@host/database?sslmode=require
DATABASE_URL=

# -----------------------------------------------------------------------------
# REDIS
# -----------------------------------------------------------------------------
REDIS_PASSWORD=GERE_UMA_SENHA_FORTE_AQUI_32_CHARS

# -----------------------------------------------------------------------------
# AWS - Credenciais
# -----------------------------------------------------------------------------
# Para EC2 com IAM Role, deixe em branco (recomendado)
# Para acesso direto, use credenciais de um IAM User com permissões mínimas
AWS_DEFAULT_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# -----------------------------------------------------------------------------
# AWS - S3
# -----------------------------------------------------------------------------
# Bucket para armazenar imagens derivadas (NDVI, COGs, etc)
S3_BUCKET=

# -----------------------------------------------------------------------------
# AWS - SQS
# -----------------------------------------------------------------------------
# Filas para processamento de jobs
SQS_QUEUE_NAME=vivacampo-prod-jobs
SQS_DLQ_NAME=vivacampo-prod-jobs-dlq

# -----------------------------------------------------------------------------
# JWT - Autenticação (OBRIGATÓRIO - gere valores únicos!)
# -----------------------------------------------------------------------------
# Use: openssl rand -base64 48
JWT_SECRET=
JWT_ISSUER=vivacampo
JWT_AUDIENCE=vivacampo-api

# -----------------------------------------------------------------------------
# JWT - System Admin
# -----------------------------------------------------------------------------
SYSTEM_ADMIN_ISSUER=vivacampo-admin
SYSTEM_ADMIN_AUDIENCE=vivacampo-admin-api

# -----------------------------------------------------------------------------
# JWT - Session
# -----------------------------------------------------------------------------
# Use: openssl rand -base64 48
SESSION_JWT_SECRET=
SESSION_JWT_ISSUER=vivacampo-session
SESSION_JWT_AUDIENCE=vivacampo-session-api

# -----------------------------------------------------------------------------
# AI/ML API Keys (opcional - preencha conforme necessário)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=

# -----------------------------------------------------------------------------
# Frontend
# -----------------------------------------------------------------------------
# URL base da API (como o frontend vê a API)
NEXT_PUBLIC_API_BASE=/api

# -----------------------------------------------------------------------------
# Domínio (para SSL)
# -----------------------------------------------------------------------------
DOMAIN=seu-dominio.com.br
